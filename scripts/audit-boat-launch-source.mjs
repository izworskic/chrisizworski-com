#!/usr/bin/env node
import fs from 'node:fs';
const LAYER='https://services3.arcgis.com/Jdnp1TjADvSDxMAX/arcgis/rest/services/PRDBASPublicView/FeatureServer/0';
const SUPPLEMENTAL=JSON.parse(fs.readFileSync('data/boat-launch-supplemental.json','utf8'));
const ACCEPTANCE=[
 ['Bay City, MI',43.5945,-83.8889],['Tawas City, MI',44.2695,-83.5147],['Alpena, MI',45.0617,-83.4328],['Mackinaw City, MI',45.7775,-84.7271],['Petoskey, MI',45.3733,-84.9553],['Ludington, MI',43.9553,-86.4526],['Holland, MI',42.7875,-86.1089],['South Haven, MI',42.4031,-86.2736],['Munising, MI',46.4111,-86.6479],['Marquette, MI',46.5436,-87.3954],['Monroe, MI',41.9164,-83.3977],['Lansing, MI',42.7325,-84.5555],['Jackson, MI',42.2459,-84.4013],['Houghton Lake, MI',44.3147,-84.7647]
];
const CONNECTING=new Set(['Detroit River','Saint Clair River','Saint Marys River']);
const rad=d=>d*Math.PI/180;function distanceMiles(a,b,c,d){const R=3958.7613;const x=Math.sin(rad(c-a)/2)**2+Math.cos(rad(a))*Math.cos(rad(c))*Math.sin(rad(d-b)/2)**2;return 2*R*Math.asin(Math.sqrt(x));}
function hasCoordinate(a){return a.latitude!==null&&a.latitude!==undefined&&String(a.latitude).trim()!==''&&a.longitude!==null&&a.longitude!==undefined&&String(a.longitude).trim()!==''&&Number.isFinite(Number(a.latitude))&&Number.isFinite(Number(a.longitude));}
function reviewStatus(a){const flag=String(a.flag||'').trim();if(!flag)return 'source-qualified';if(flag==='InProgress')return 'dnr-review-in-progress';return 'withhold';}
function scopeOf(a){return String(a.greatlakesaccess||'').startsWith('Yes')||CONNECTING.has(String(a.waterbody||'').trim())?'great-lakes':'inland-or-other';}
function nearby(records,lat,lon,radius=25){return records.map(a=>({...a,_mi:distanceMiles(lat,lon,Number(a.latitude),Number(a.longitude))})).filter(a=>a._mi<=radius).sort((a,b)=>a._mi-b._mi);}
async function fetchJson(url){const r=await fetch(url,{headers:{accept:'application/json','user-agent':'ChrisIzworskiBoatLaunchAudit/4.0 (+https://chrisizworski.com/michigan-boat-launches/)'},signal:AbortSignal.timeout(20000)});if(!r.ok)throw new Error(`HTTP ${r.status} for ${url}`);const j=await r.json();if(j?.error)throw new Error(j.error.message||'ArcGIS query error');return j;}
function queryUrl(params){return `${LAYER}/query?${new URLSearchParams({...params,f:'json'})}`;}
const fields='OBJECTID,globalid,facilityid,name,waterbody,bas_type,launch_status,condition,greatlakesaccess,referenceonly,flag,flagcomments,latitude,longitude,ntrailerableparking,rampcode_new';
const [metadata,total,recordsResponse]=await Promise.all([fetchJson(`${LAYER}?f=json`),fetchJson(queryUrl({where:'1=1',returnCountOnly:'true'})),fetchJson(queryUrl({where:"bas_type='Boating Access Site'",outFields:fields,returnGeometry:'false',resultRecordCount:'2000'}))]);
if(recordsResponse.exceededTransferLimit===true)throw new Error('DNR source audit was truncated');
const records=(recordsResponse.features||[]).map(f=>f.attributes||{});const open=records.filter(a=>a.launch_status==='Open');
const eligible=open.filter(a=>String(a.referenceonly||'').toLowerCase()!=='yes'&&hasCoordinate(a)&&a.name);
const sourceQualified=eligible.filter(a=>reviewStatus(a)==='source-qualified');const reviewInProgress=eligible.filter(a=>reviewStatus(a)==='dnr-review-in-progress');const withheld=eligible.filter(a=>reviewStatus(a)==='withhold');const usableDnr=[...sourceQualified,...reviewInProgress];
const municipal=SUPPLEMENTAL.filter(a=>a.sourceType==='municipal-supplemental'&&a.verificationStatus==='municipal-source-qualified'&&a.id&&a.name&&a.operator&&a.sourceUrl&&a.coordinateSourceUrl&&hasCoordinate(a));
const sourceBacked=[...usableDnr,...municipal];const greatLakes=usableDnr.filter(a=>scopeOf(a)==='great-lakes');const inland=usableDnr.filter(a=>scopeOf(a)==='inland-or-other');
const conditionCounts=usableDnr.reduce((counts,a)=>{const key=String(a.condition||'').trim()||'Not reported';counts[key]=(counts[key]||0)+1;return counts;},{});
console.log('Michigan statewide boat launch source audit');console.log(`DNR layer last edit: ${metadata?.editingInfo?.lastEditDate?new Date(metadata.editingInfo.lastEditDate).toISOString():'unknown'}`);console.log(`Total layer: ${total.count}`);console.log(`Boating access sites: ${records.length}`);console.log(`Open: ${open.length}`);console.log(`Usable DNR statewide: ${usableDnr.length}`);console.log(`Great Lakes / connecting: ${greatLakes.length}`);console.log(`Inland / other: ${inland.length}`);console.log(`Municipal supplements: ${municipal.length}`);console.log(`Withheld review state: ${withheld.length}`);console.log(`DNR facility condition values: ${JSON.stringify(conditionCounts)}`);
let zero=0;const coverage=[];for(const [name,lat,lon] of ACCEPTANCE){const n=nearby(sourceBacked,lat,lon);if(!n.length)zero++;coverage.push({destination:name,count:n.length,nearest:n.slice(0,6).map(a=>({name:a.name,miles:Number(a._mi.toFixed(2)),scope:a.sourceType==='municipal-supplemental'?'municipal':scopeOf(a)}))});console.log(`${name}: ${n.length} within 25 mi${n[0]?` · nearest ${n[0].name} ${n[0]._mi.toFixed(1)} mi`:''}`);}
const falseBayCity=[...records,...SUPPLEMENTAL].filter(a=>String(a.name||'').trim().toLowerCase()==='bay city state park launch');
const report={generatedAt:new Date().toISOString(),dnrLayerLastEdit:metadata?.editingInfo?.lastEditDate||null,counts:{totalLayer:total.count,boatingAccessSites:records.length,open:open.length,usableDnr:usableDnr.length,greatLakes:greatLakes.length,inlandOrOther:inland.length,municipal:municipal.length,sourceBacked:sourceBacked.length,withheld:withheld.length},conditionCounts,coverage,falseBayCityStateParkRecords:falseBayCity,failures:{falseBayCityStatePark:falseBayCity.length>0,implausibleCounts:records.length<500||open.length<300||usableDnr.length<500||greatLakes.length<50||inland.length<100,zeroCoverageDestinations:coverage.filter(x=>x.count===0).map(x=>x.destination)}};
fs.mkdirSync('artifacts',{recursive:true});fs.writeFileSync('artifacts/boat-launch-source-audit.json',JSON.stringify(report,null,2));console.log('Diagnostic report: artifacts/boat-launch-source-audit.json');
if(report.failures.falseBayCityStatePark)throw new Error('Known false Bay City State Park Launch appears in source inventory');if(report.failures.implausibleCounts)throw new Error('DNR statewide source audit returned implausibly low counts');if(report.failures.zeroCoverageDestinations.length)throw new Error(`Zero statewide launch coverage within 25 miles for: ${report.failures.zeroCoverageDestinations.join(', ')}`);