name: Fix Ballard Tour Freshness

on:
  push:
    branches:
      - agent/ballard-interactive-tour-production
    paths:
      - '.github/workflows/fix-ballard-tour-freshness.yml'

permissions:
  contents: write

jobs:
  freshness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ballard-interactive-tour-production
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Stamp freshness
        run: node scripts/stamp-freshness.mjs
      - name: Verify freshness
        run: npm run freshness -- --check
      - name: Commit freshness alignment
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          if git diff --quiet; then
            echo 'Freshness already aligned.'
            exit 0
          fi
          git add public
          git commit -m 'Align Ballard tour freshness metadata'
          git push
