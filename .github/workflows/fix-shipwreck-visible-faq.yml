name: Fix shipwreck visible FAQ consistency
on:
  push:
    branches: [agent/decision-network-boat-shipwreck]
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/decision-network-boat-shipwreck
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/fix-shipwreck-visible-faq.mjs
      - run: rm scripts/fix-shipwreck-visible-faq.mjs .github/workflows/fix-shipwreck-visible-faq.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "Align visible shipwreck access wording"
          git push origin HEAD:agent/decision-network-boat-shipwreck
