name: Ship Ballard single-map AIS

on:
  push:
    branches:
      - agent/ballard-single-map-production
    paths:
      - '.github/workflows/ballard-single-map-production.yml'
      - 'scripts/patch-ballard-single-map-production.py'

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ballard-single-map-production
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Patch production integration
        run: python scripts/patch-ballard-single-map-production.py
      - name: Install authoritative Ballard package
        run: npm install --ignore-scripts
      - name: Sync Ballard package into site
        run: node scripts/sync-ballard-locks.mjs
      - name: Full repository gate
        run: npm run verify:all
      - name: Commit production integration
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package.json package-lock.json scripts/sync-ballard-locks.mjs public/ballard-locks public/sitemap.xml api/ballard-locks.js api/ballard-ais.js tests/ballard-locks-extracted.test.js scripts/ballard-production-smoke.mjs
          git commit -m 'Ship Ballard single-map live AIS'
          git push
