(()=>{
  "use strict";
  const $=id=>document.getElementById(id);
  const esc=value=>String(value==null?"":value).replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[ch]));
  const GATEWAYS={
    asheville:{label:"Asheville",mile:"MP 384",page:"/blue-ridge-parkway/asheville/",fallback:"North toward Craggy Gardens or south toward Mount Pisgah — closures, visibility and the clock decide which one earns today."},
    boone:{label:"Boone / Blowing Rock",mile:"MP 291",page:"/blue-ridge-parkway/boone/",fallback:"The High Country packs Rough Ridge, Linn Cove Viaduct, Moses Cone and Linville Falls into a compact band."},
    roanoke:{label:"Roanoke",mile:"MP 121",page:"/blue-ridge-parkway/roanoke/",fallback:"Peaks of Otter is the clean northern play; Rocky Knob and Mabry Mill create a longer historic drive south."},
    cherokee:{label:"Cherokee",mile:"MP 469",page:"/blue-ridge-parkway/cherokee/",fallback:"Waterrock Knob is the short target. More time opens the high Balsam country toward Richland Balsam and Mount Pisgah."}
  };
  const track=(name,params={})=>{try{if(typeof window.gtag==="function")window.gtag("event",name,params);}catch{}};
  function parkwayDate(){return new Intl.DateTimeFormat("en-CA",{timeZone:"America/New_York",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());}
  function startClock(){const parts=new Intl.DateTimeFormat("en-US",{timeZone:"America/New_York",hour:"2-digit",minute:"2-digit",hour12:false}).formatToParts(new Date()),hour=Number(parts.find(x=>x.type==="hour")?.value||9);if(hour<8)return"09:00";if(hour>=17)return"09:00";return`${String(hour).padStart(2,"0")}:00`;}
  function apiUrl(gateway,hours=4,interests="scenery,short-walk"){const q=new URLSearchParams({gateway,finish:"return",hours:String(hours),date:parkwayDate(),start:startClock(),interests});return`/api/blue-ridge-parkway?${q}`;}
  async function fetchSnapshot(gateway,hours=4,interests="scenery,short-walk"){
    const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),9000);
    try{const response=await fetch(apiUrl(gateway,hours,interests),{headers:{Accept:"application/json"},signal:controller.signal});if(!response.ok)throw new Error(`HTTP ${response.status}`);const payload=await response.json();if(!payload?.selected)throw new Error("No selected route");return payload;}finally{clearTimeout(timer);}
  }
  function selectedReason(plan){const why=Array.isArray(plan?.why)?plan.why.filter(Boolean):[];return why[0]||plan?.editorial?.text||plan?.character||"The route engine chose this section from the open, time-feasible candidates.";}
  function state(payload){const plan=payload?.selected;if(!payload?.road?.ok)return{label:"VERIFY ROAD",warn:true};if(plan?.cautions?.length)return{label:"ROAD NOTE",warn:true};if(String(plan?.fit||"").toLowerCase().includes("tight"))return{label:"TIGHT FIT",warn:true};return{label:"BEST FIT",warn:false};}
  function renderSnapshotCard(gateway,payload){const g=GATEWAYS[gateway],plan=payload.selected,s=state(payload),end=plan.timeline?.returnBy||plan.timeline?.arrivalBy||"—",direction=plan.direction==="northbound"?"North":"South";return`<article class="snapshot-card" data-state="${s.warn?"warn":"good"}"><div class="snapshot-kicker"><span>${esc(g.mile)} · ${esc(g.label)}</span><span class="snapshot-state">${esc(s.label)}</span></div><h3>${esc(plan.name||`${direction} on the Parkway`)}</h3><p class="snapshot-route">${esc(direction)} · ${esc(plan.durationHours||4)} hr · ${esc(plan.routeMiles||"—")} Parkway mi · back about ${esc(end)}</p><p>${esc(selectedReason(plan))}</p><div class="snapshot-meta"><span>${payload.road?.ok?"NPS road table loaded":"Road status degraded"}</span><span>View outlook: ${esc(plan.viewOutlook?.label||"unknown")}</span></div><div class="snapshot-actions"><button type="button" data-use-gateway="${esc(gateway)}">Build this drive</button><a href="${esc(g.page)}">From ${esc(g.label)} →</a></div></article>`;}
  function renderSnapshotFallback(gateway,error){const g=GATEWAYS[gateway];return`<article class="snapshot-card" data-state="warn"><div class="snapshot-kicker"><span>${esc(g.mile)} · ${esc(g.label)}</span><span class="snapshot-state">LIVE CHECK DEGRADED</span></div><h3>Use the planner before you leave.</h3><p class="snapshot-route">Current route snapshot could not be loaded.</p><p>${esc(g.fallback)}</p><div class="snapshot-actions"><button type="button" data-use-gateway="${esc(gateway)}">Build from ${esc(g.label)}</button><a href="${esc(g.page)}">Gateway guide →</a></div></article>`;}
  function bindSnapshotButtons(){document.querySelectorAll("[data-use-gateway]").forEach(btn=>btn.addEventListener("click",()=>{const gateway=btn.dataset.useGateway;if(!$("plannerForm")){location.href=`/blue-ridge-parkway/?gateway=${encodeURIComponent(gateway)}&hours=4&interests=scenery,short-walk#planner`;return;}applyPlannerState({gateway,hours:"4",interests:["scenery","short-walk"]});track("blue_ridge_snapshot_to_planner",{gateway});$("plannerForm").scrollIntoView({behavior:"smooth",block:"start"});setTimeout(()=>$("buildDrive")?.click(),180);}));}
  function applyPlannerState({gateway,hours,interests,finish}={}){if(gateway&&$("gateway"))$("gateway").value=gateway;if(finish&&$("finish"))$("finish").value=finish;if(hours&&$("hours")){const target=String(hours);if([...$("hours").options].some(o=>o.value===target))$("hours").value=target;}if(Array.isArray(interests)&&interests.length){document.querySelectorAll('input[name="interest"]').forEach(input=>input.checked=interests.includes(input.value));}$("gateway")?.dispatchEvent(new Event("change",{bubbles:true}));$("hours")?.dispatchEvent(new Event("change",{bubbles:true}));}
  function hydrateQueryState(){if(!$("plannerForm"))return;const q=new URLSearchParams(location.search),gateway=q.get("gateway"),finish=q.get("finish"),hours=q.get("hours"),interests=(q.get("interests")||"").split(",").filter(Boolean);if(gateway&&GATEWAYS[gateway]||["floyd","afton"].includes(gateway))applyPlannerState({gateway,finish,hours,interests});}
  async function hydrateDesk(){const grid=$("gatewaySnapshots");if(!grid)return;const entries=Object.keys(GATEWAYS),results=await Promise.allSettled(entries.map(g=>fetchSnapshot(g)));let ok=0,warn=0,updated="";grid.innerHTML=results.map((result,index)=>{const gateway=entries[index];if(result.status==="fulfilled"){ok++;const payload=result.value;if(state(payload).warn)warn++;updated=updated||payload.road?.updatedLabel||"";return renderSnapshotCard(gateway,payload);}warn++;return renderSnapshotFallback(gateway,result.reason);}).join("");const summary=$("parkwayNowSummary");if(summary){summary.textContent=ok===0?"The live route snapshots are degraded. The full planner can still retry the official road and weather checks.":warn?`${ok} gateway checks loaded · ${warn} need a road or fit note${updated?` · NPS ${updated}`:""}.`:`All ${ok} gateway checks loaded cleanly${updated?` · NPS ${updated}`:""}.`;}const freshness=$("deskFreshness");if(freshness)freshness.textContent=`Live gateway checks generated ${new Date().toLocaleTimeString([], {hour:"numeric",minute:"2-digit"})}`;bindSnapshotButtons();track("blue_ridge_gateway_desk_loaded",{loaded:ok,warnings:warn});}
  function renderGatewayLive(gateway,payload){const box=$("gatewayLive");if(!box)return;const g=GATEWAYS[gateway],plan=payload.selected,s=state(payload),direction=plan.direction==="northbound"?"North":"South",end=plan.timeline?.returnBy||plan.timeline?.arrivalBy||"—";box.innerHTML=`<div class="eyebrow">LIVE ROUTE READ · ${esc(s.label)}</div><h2>${esc(plan.name)}</h2><p>${esc(selectedReason(plan))}</p><div class="snapshot-meta"><span>${esc(direction)}</span><span>${esc(plan.durationHours)} hr</span><span>${esc(plan.routeMiles)} Parkway mi</span><span>Back about ${esc(end)}</span><span>Views: ${esc(plan.viewOutlook?.label||"unknown")}</span></div><div class="snapshot-actions"><a class="primary-link" href="/blue-ridge-parkway/?gateway=${encodeURIComponent(gateway)}&hours=4&interests=scenery,short-walk#planner">Build a route from ${esc(g.label)}</a></div><p style="font-size:10px;margin-top:14px">${payload.road?.ok?`Official NPS road table loaded${payload.road.updatedLabel?` · ${esc(payload.road.updatedLabel)}`:""}.`:"Official road-status check was unavailable. Verify NPS before travel."}</p>`;}
  async function hydrateGatewayPage(){const gateway=document.body?.dataset?.blueRidgeGateway;if(!gateway||!GATEWAYS[gateway]||!$("gatewayLive"))return;try{const payload=await fetchSnapshot(gateway);renderGatewayLive(gateway,payload);track("blue_ridge_gateway_page_live",{gateway,ok:true});}catch(error){const g=GATEWAYS[gateway];$("gatewayLive").innerHTML=`<div class="eyebrow">LIVE CHECK DEGRADED</div><h2>Use the route planner before you leave.</h2><p>${esc(g.fallback)}</p><div class="snapshot-actions"><a class="primary-link" href="/blue-ridge-parkway/?gateway=${encodeURIComponent(gateway)}&hours=4&interests=scenery,short-walk#planner">Open the ${esc(g.label)} planner</a></div><p style="font-size:10px;margin-top:14px">Current live snapshot could not be loaded. Do not infer that the road is open.</p>`;track("blue_ridge_gateway_page_live",{gateway,ok:false});}}
  document.addEventListener("DOMContentLoaded",()=>{hydrateQueryState();hydrateDesk();hydrateGatewayPage();});
})();