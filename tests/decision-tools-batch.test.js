import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";const b=JSON.parse(await readFile(new URL("../benchmarks/decision-tools-batch-2026-08-26.json",import.meta.url),"utf8"));test("decision tool batch clears build scores and declares two loss functions",()=>{assert.equal(b.tools.length,6);assert.equal(Object.keys(b.lossFunctions).length,2);for(const t of b.tools){assert.ok(t.bestFitScore>=b.buildThreshold,t.id);assert.ok(t.liveSources.length>=2,t.id);assert.equal(t.measurement.windowDays,28)}});